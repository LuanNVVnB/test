 const JWT_TOKEN_SECRET = "secret";
 const CLIENT_CALLBACK_JWT = "http://localhost:5000/#/redirect?token=";
 const BEARER = "Bearer";

module.exports = {
    JWT_TOKEN_SECRET,
    CLIENT_CALLBACK_JWT,
    BEARER
}
